# ======================
# Supabase Database
# ======================
# Get these from: Supabase Dashboard → Project Settings → Database
#
# Transaction-mode pooler (recommended for serverless/backend):
#   Uses port 6543, connections shared per-transaction via PgBouncer.
DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
#
# Direct connection (for migrations or session-mode needs):
#   Uses port 5432, dedicated connection per client.
# DATABASE_URL=postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres
#
# Set to false if using direct connection (port 5432).
# When true, pgx uses simple protocol for PgBouncer compatibility.
DB_USE_POOLER=true

# Fallback: individual params (used when DATABASE_URL is empty, e.g. local dev)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_NAME=ai_companion
# DB_SSL_MODE=disable

# ======================
# Server
# ======================
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
SERVER_READ_TIMEOUT=15s
SERVER_WRITE_TIMEOUT=15s

# ======================
# JWT
# ======================
JWT_SECRET=change-me-in-production
JWT_EXPIRATION=24h

# ======================
# OpenAI
# ======================
OPENAI_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# ======================
# CORS
# ======================
CORS_ALLOWED_ORIGINS=http://localhost:3000
